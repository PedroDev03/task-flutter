server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Garante que as rotas internas do Flutter funcionem ao recarregar a página
        try_files $uri $uri/ /index.html;
    }
}